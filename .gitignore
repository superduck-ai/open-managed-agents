# OS/editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
*~

# Local environment and secrets
.env
.env.*
/config/config.yaml
/config/config.local.yaml
/deploy/docker-compose/oma-server.local.yaml

# Logs and runtime files
*.log
*.pid
*.seed
*.tmp
tmp/
temp/

# Go build/test artifacts
/bin/
/open-managed-agents
*.test
*.out
*.prof
coverage.out
coverage.html

# Frontend dependencies and build artifacts
node_modules/
web/node_modules/
dist/
web/dist/
.vite/
web/.vite/
coverage/
web/coverage/
playwright-report/
web/playwright-report/
test-results/
web/test-results/
*.tsbuildinfo

# Local databases and object storage scratch data
*.db
*.sqlite
*.sqlite3
minio-data/
data/

# Large bundled assets kept out of the repo for now
assets/

# envd binaries — extracted at runtime by init-envd container from
# ghcr.io/superduck-ai/e2b-local image.  Docker daemon needs host-accessible
# envd to bind-mount into sandbox containers.
envd-bin/
logs/

# AI agent 个人本地覆盖文件（不提交）
# Claude Code 读 CLAUDE.local.md；Codex 读 AGENTS.override.md（覆盖同级 AGENTS.md）。
# Codex 全局个人偏好放 ~/.codex/AGENTS.override.md，不在此处。
CLAUDE.local.md
AGENTS.override.md
.agents/
