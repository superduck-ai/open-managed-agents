<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="CodeSessionInboundEventMapper">
    <select id="ListExistingActivationInboundEvents"
            resultType="codeSessionInboundEventIdentityRow">
        SELECT
            code_session_external_id,
            idempotency_key
        FROM code_session_inbound_events
        WHERE organization_uuid = #{organizationUUID}
          AND workspace_uuid = #{workspaceUUID}
          AND idempotency_key IN
          <foreach collection="idempotencyKeys" item="idempotencyKey"
                   open="(" separator="," close=")">
              #{idempotencyKey}
          </foreach>
          AND deleted_at IS NULL
    </select>

    <insert id="InsertCodeSessionInboundEvents" result="rows">
        INSERT INTO code_session_inbound_events (
            external_id,
            organization_uuid,
            workspace_uuid,
            code_session_uuid,
            code_session_external_id,
            sequence_num,
            event_type,
            event_subtype,
            payload_uuid,
            request_id,
            payload,
            payload_hash,
            idempotency_key,
            delivery_status,
            source,
            created_at,
            updated_at
        ) VALUES
        <foreach collection="rows" item="row" separator=",">
            (
                #{row.ExternalID},
                #{row.OrganizationUUID},
                #{row.WorkspaceUUID},
                #{row.CodeSessionUUID},
                #{row.CodeSessionExternalID},
                #{row.SequenceNum},
                #{row.EventType},
                #{row.EventSubtype},
                #{row.PayloadUUID},
                #{row.RequestID},
                CAST(#{row.Payload} AS jsonb),
                #{row.PayloadHash},
                #{row.IdempotencyKey},
                #{row.DeliveryStatus},
                #{row.Source},
                #{row.CreatedAt},
                #{row.CreatedAt}
            )
        </foreach>
    </insert>
</mapper>
