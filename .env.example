# Open Managed Agents — docker compose 环境变量（可选）
# 复制此文件为 .env 并编辑后，docker compose 会自动加载。
#
# 所有变量都是可选的 —— compose 内置了合理的开发默认值。

# ---- 上游 Anthropic API（让 oma-server 调用 Claude）----
# Server-only upstream credential; never inject this value into a sandbox.
# ANTHROPIC_UPSTREAM_API_KEY=sk-ant-...
# ANTHROPIC_UPSTREAM_BASE_URL=https://your-proxy.example.com

# ---- Code session ingress JWT signing ----
# Production requires an Ed25519 private key in PKCS#8 PEM format.
# Development and tests use one process-scoped ephemeral key when omitted.
# Generate one with: just generate-code-session-jwt-key /run/secrets/code-session-jwt-ed25519.pem
# CODE_SESSION_JWT_SIGNING_KEY_FILE=/run/secrets/code-session-jwt-ed25519.pem

# ---- CCRv2 HTTPS MITM（可选）----
# 私钥由部署侧长期稳定保存并以只读 Secret 挂载。根证书由 oma-server 每次启动
# 重新签发并只保存在内存，不需要配置证书输出路径或可写目录。
# 默认 docker-compose.yml 尚未透传这些变量或挂载对应路径；容器部署需要在
# compose override 中同时映射环境变量和只读私钥。
# 可先运行 ./scripts/generate-upstream-proxy-ca-key.sh <安全输出路径> 生成稳定私钥。
# CODE_SESSION_UPSTREAM_PROXY_MITM_ENABLED=true
# CODE_SESSION_UPSTREAM_PROXY_CA_KEY_FILE=/run/secrets/ccrv2-mitm-ca-key.pem

# ---- 前端访问端口 ----
# Caddy 监听端口（默认 28080）。端口冲突时可改为其他端口，或设为 0 使用随机端口。
# CADDY_HOST_PORT=28080
