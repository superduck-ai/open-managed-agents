---
title: "API overview"
description: "Use the OMA `/v1` API to create and run managed agents."
icon: "brackets-curly"
---

OMA provides a resource-oriented HTTP API and aims to preserve Anthropic SDK request, response, and error shapes. The default local base URL is:

```text
http://localhost:38080
```

Public resource endpoints are under `/v1`. Console routes under `/api`, `/auth`, and `/web-api` are not part of the public SDK contract.

## First request

```bash
curl http://localhost:38080/v1/models \
  -H 'Authorization: Bearer sk-ant-local-default'
```

You can also use `X-Api-Key`:

```bash
curl http://localhost:38080/v1/models \
  -H 'X-Api-Key: sk-ant-local-default'
```

## Common headers

| Header | Requirement | Purpose |
| --- | --- | --- |
| `Authorization: Bearer <key>` | Choose one | Bearer API key |
| `X-Api-Key: <key>` | Choose one | API key header commonly used by Anthropic SDKs |
| `Content-Type: application/json` | JSON requests | Request body encoding |
| `anthropic-version: 2023-06-01` | Recommended for SDK-compatible calls | API compatibility version |
| `anthropic-beta` | Resource-specific | Enables selected beta contracts |

<Warning>
  OMA does not have one universal beta request template. Some resources require `?beta=true`, while others also validate a resource-specific `anthropic-beta` value. Follow each resource page.
</Warning>

## Core resources

<CardGroup cols={2}>
  <Card title="Models" icon="microchip" href="/en/api/endpoints/list-models">
    Inspect the models and capabilities exposed by this deployment.
  </Card>
  <Card title="Agents" icon="robot" href="/en/api/endpoints/create-agent">
    Manage versioned model, tool, and instruction definitions.
  </Card>
  <Card title="Sessions" icon="messages" href="/en/api/endpoints/create-session">
    Create long-running, stateful agent tasks.
  </Card>
  <Card title="Environments" icon="box">
    Manage sandbox environments, work items, and runtime configuration.
  </Card>
  <Card title="Files and skills" icon="files">
    Manage inputs, task artifacts, and reusable skills.
  </Card>
  <Card title="Deployments" icon="play">
    Manage repeatable deployments and deployment runs.
  </Card>
  <Card title="Memory stores" icon="brain">
    Persist and version durable memory across sessions.
  </Card>
  <Card title="Vaults and webhooks" icon="shield-keyhole">
    Protect credentials and forward state events to other systems.
  </Card>
</CardGroup>

The server also implements files, message batches, organizations, session resources, threads, events, and filestore APIs. This first reference focuses on Models, Agents, and Sessions. Later versions will add each remaining resource after its schema has been checked against the implementation.

## Responses and tracing

Successful responses use JSON. The service returns a `request-id` header and includes the same identifier in error bodies for log correlation.

```json
{
  "type": "error",
  "request_id": "req_...",
  "error": {
    "type": "invalid_request_error",
    "message": "..."
  }
}
```

<CardGroup cols={3}>
  <Card title="Authentication" icon="key" href="/en/api/authentication" />
  <Card title="Pagination" icon="list" href="/en/api/pagination" />
  <Card title="Errors" icon="triangle-exclamation" href="/en/api/errors" />
</CardGroup>
