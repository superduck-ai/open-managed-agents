<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="SessionThreadMapper">
    <sql id="columns">
        uuid,
        external_id,
        organization_uuid,
        workspace_uuid,
        session_uuid,
        session_external_id,
        parent_thread_uuid,
        parent_thread_external_id,
        agent_snapshot,
        status,
        usage,
        stats,
        created_at,
        updated_at,
        archived_at,
        deleted_at
    </sql>

    <select id="GetPrimarySessionThread" resultType="sessionThreadRow">
        SELECT
        <include refid="columns"/>
        FROM session_threads
        WHERE workspace_uuid = #{workspaceUUID}
          AND session_external_id = #{sessionExternalID}
          AND parent_thread_uuid IS NULL
          AND deleted_at IS NULL
        ORDER BY created_at ASC, uuid ASC
        LIMIT 1
    </select>

    <select id="GetSessionThreadByExternalID" resultType="sessionThreadRow">
        SELECT
        <include refid="columns"/>
        FROM session_threads
        WHERE workspace_uuid = #{workspaceUUID}
          AND session_external_id = #{sessionExternalID}
          AND external_id = #{threadExternalID}
          AND deleted_at IS NULL
    </select>
</mapper>
